package it.bestclient.android.RatingModel;

/* simple interface to guarantee that every rating object use the same interface*/
public interface Voto {
     void setVoto(double voto);
     double getVoto();
}
